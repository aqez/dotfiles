#!/usr/bin/env bash
set -euo pipefail

if [ $# -ne 1 ]; then
  echo "Usage: pr-review <pr-number>" >&2
  exit 1
fi

gh pr view "$1" --web

#pi --model github-copilot/claude-opus-4.6 --name "PR Review $1" "/skill:pr-review $1"
pi --model "local/llama-cpp" --name "PR Review $1" "/skill:pr-review $1"
